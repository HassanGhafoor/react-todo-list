import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/app';

// Renders the App component into the #app div in index.html
ReactDOM.render(<App />, document.getElementById('app'));
